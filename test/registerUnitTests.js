var expect    = require("chai").expect;
//var converter = require("../src/inverter");

describe("Registering Instances", function() {
  it("can just register an instance", function() {
    expect(true).to.equal(true);
  });
});
